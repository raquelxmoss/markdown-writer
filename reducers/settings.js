const defaultSettings = {
  fontFamily: 'Rokkitt',
  color: '#3b3b3b',
  lineHeight: '120%',
  fontSize: '1em',
  background: '#fefefe'
}

export const settings = (state = defaultSettings, action) => {
  return state
}